#include <stdio.h>
#include <stdlib.h>

int heapsort(long int v[], long int i, int n) {

}

int main() {
    long int v*;
    int n;

    scanf("%d", &n);
    printf("%d", n);

    v = (long int *) malloc(n*sizeof(long int));

    return 0;
}
